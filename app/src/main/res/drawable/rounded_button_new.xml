<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền xanh nhạt -->
    <solid android:color="#3399CC" /> <!-- Màu xanh nhạt -->

    <!-- Bo góc -->
    <corners android:radius="16dp" />

    <!-- Viền (nếu cần) -->
    <stroke
        android:width="2dp"
        android:color="#80DEEA" /> <!-- Viền xanh nhạt hơn -->
</shape>
